# vc-disableCompression
Disables compression on uploading a file (ex: webp, png, jpg).

# How to Install
Run this command into your Vencord Folder
```bash
cd src/userplugins
git clone https://github.com/Zv-yz/vc-disableCompression
pnpm build
```